// exercise 12
var members = ['ali', 'atif', 'shadan', 'sara'];
var message = 'kal kon se class hai';
for (var index = 0; index < members.length; index++) {
    console.log(message + members[index]);
}
